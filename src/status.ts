export enum GameStatus {
  WIN,
  LOSE,
  PLAY,
}

export enum MouseStatus {
  DOWN,
  ENTER,
  LEAVE,
  MOVE,
  OUT,
  OVER,
  UP,
}

export enum FaceStatus {
  Normal,
  PressBlock,
  PressNormal,
  Win,
  Lose,
}
